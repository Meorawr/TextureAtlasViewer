---
name: Generate Atlases
on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 18 * * *'
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Generate Atlases
        run: make data

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v4
        with:
          commit-message:  Regenerate atlas databases
          branch: actions/generate-atlates
          delete-branch: true
          title: Regenerate atlas databases
          body: Automated generation of atlas data.
          reviewers: Meorawr
